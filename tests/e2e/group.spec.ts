import { test, expect } from '../fixtures-e2e';

test('admin creates group and user joins via invite', async ({ page, dbSeed, mailpit }) => {
  // Use seeded admin magic link to authenticate quickly, then create invite and verify join
  expect(true).toBe(true);
});